    import XCTest
    @testable import BuildTheWorld

    final class BuildTheWorldTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(BuildTheWorld().text, "Hello, World!")
        }
    }
